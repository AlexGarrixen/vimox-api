import { DB_NAME, DB_USER, DB_HOST, DB_PASSWORD } from '../shared/env';

const settings = {
  database: DB_NAME,
  host: DB_HOST,
  username: DB_USER,
  password: DB_PASSWORD,
};

export default settings;
